<header>
  <h1>
    <%=link_to "Pixel Exhibition",root_path, class:"header-title"%>
  </h1>
  <link href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
    <%= form_with(url: search_posts_path, local: true, method: :get, class:"search_container") do |form| %>
      <%= form.text_field :name, placeholder: "キーワード検索" %>
      <input type="submit" value="&#xf002">
    <% end %>
  <nav class="pc-nav">
    <ul class="header-ul">
      <% if user_signed_in?%>
      <li class="header-list"><%=link_to "TOP", root_path, class:"list"%></li>
      <li class="header-list"><%=link_to "アップロード", new_post_path, class:"upload" %></li>
      <li class="header-list"><%=image_tag current_user.image,class:"icon_image" if current_user.image.attached?%></li>
      <li class="header-list"><%=link_to current_user.nickname, user_path(current_user.id), class:"list"%></li>
      <li class="header-list"><%=link_to "ログアウト", destroy_user_session_path, method: :delete, class:"list" %></li>
      <% else %>
      <li class="header-list"><%=link_to "アップロード", new_user_registration_path, class:"upload" %></li>
      <li class="header-list"><%=link_to "ログイン", new_user_session_path, class:"list" %></li>
      <li class="header-list"><%=link_to "新規登録", new_user_registration_path, class:"list" %></li>
      <% end %>
    </ul>
  </nav>
</header>


